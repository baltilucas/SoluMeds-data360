import { ComponentFixture, TestBed } from '@angular/core/testing';
import { IngresaralergiaPage } from './ingresaralergia.page';

describe('IngresaralergiaPage', () => {
  let component: IngresaralergiaPage;
  let fixture: ComponentFixture<IngresaralergiaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(IngresaralergiaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
