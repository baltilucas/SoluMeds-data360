import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearalergiaPage } from './crearalergia.page';

describe('CrearalergiaPage', () => {
  let component: CrearalergiaPage;
  let fixture: ComponentFixture<CrearalergiaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearalergiaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
